version: "3.7"

services:


# frontend stuff
  frontend:
    build: .                             # use Dockerfile dir
    container_name: frontend_mc_web_lieh
    restart: on-failure:5
    ports:
      - 30000:30000
    networks:
      frontend:
        ipv4_address: 172.16.1.2


networks:
  frontend:
    external: true
      
